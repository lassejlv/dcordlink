<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./assets/css/dist.css" />
    <script src="assets/js/script.js" defer></script>
    <title>Dcordlink</title>
  </head>
  <body>
    <div
      class="min-h-screen text-center justify-center items-center bg-dark-100"
    >
      <div class="p-[10rem]">
        <h2 class="text-gray-100 text-5xl font-bold mb-2">
          <em>Dcordlink.ml</em>
        </h2>
        <p class="text-gray-400 font-light mb-3">
          Get your own custom vanity url for your discord server, with analytics
          & more.
        </p>

        <% if (user) { %>
        <button
          class="btn-green uppercase font-bold"
          onclick="redirect('/dash')"
        >
          Go to dashboard
        </button>

        <% } else { %>
        <button
          class="btn-blue uppercase font-bold"
          onclick="redirect('/auth/discord')"
        >
          Login through Discord
        </button>
        <% } %>
      </div>
    </div>
  </body>
</html>
